# Simple Interest Calculator

A Bash script project built for the Git and GitHub course. This repository
also includes standard open-source project files: a license, a code of
conduct, and contribution guidelines.

## What it does

`simple-interest.sh` is an interactive command-line calculator that computes
**simple interest** based on three values the user provides at runtime.

### Formula
